import { FormatTextsPipe } from './format-texts.pipe';

describe('FormatTextsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextsPipe();
    expect(pipe).toBeTruthy();
  });
});
